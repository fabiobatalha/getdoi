API_URL = "http://doi.crossref.org/servlet/"

# A valid e-mail registered on Crossref to do queries.
# take a look at: http://www.crossref.org/requestaccount/
QUERY_EMAIL = 'support@crossref.org'

# Crossref user and password for DOI requests
# Some tests requires a valid username and password registered at crossref sistem.
USER = 'xxx'
PASSWD = 'xxx'
